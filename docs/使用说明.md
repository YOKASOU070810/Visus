# AI辅助出行 - 智能导航系统

面向视障人士的 AI 辅助出行系统，支持盲道导航、过马路辅助、红绿灯检测、物品搜索、AI 语音对话。

---

## 系统要求

- Python 3.10/3.11（推荐，3.13 需额外处理）
- NVIDIA GPU + CUDA 11.8（可选，CPU 模式较慢）
- Android 8.0+ 手机
- 阿里云 DashScope API Key

## 快速开始

### 1. 安装 Python 依赖

```bash
pip install -r requirements.txt
pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 --index-url https://download.pytorch.org/whl/cu118
```

如果使用 CPU 模式：
```bash
pip install torch torchvision
```

Windows 上 pyaudio 可能需要手动安装：
https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio

### 2. 配置 API Key

本项目的 ASR 语音识别和 AI 对话调用阿里云 DashScope。先到控制台申请 API Key：

申请地址：https://dashscope.console.aliyun.com/

然后在项目根目录创建或编辑 `.env` 文件，填入你的 DashScope API Key：
```
DASHSCOPE_API_KEY=你的Key
```

示例：
```
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
```

Windows PowerShell 也可以临时设置环境变量后再启动：
```powershell
$env:DASHSCOPE_API_KEY="sk-xxxxxxxxxxxxxxxx"
python app_main.py
```

注意：不要把真实 API Key 提交到公开仓库；`.env` 只保存在本机即可。

### 3. 启动服务器

```bash
python app_main.py
```

服务器运行在 http://localhost:8081

### 4. 安装手机 App

将 `AIGlass-demo.apk` 安装到 Android 手机。

- 打开 App → 点击右上角齿轮图标
- 输入电脑的局域网 IP 地址和端口 8081
- 保存后自动连接

### 5. 使用语音指令

| 指令 | 功能 |
|---|---|
| "开始导航" / "盲道导航" | 启动盲道导航 |
| "开始过马路" | 启动过马路模式 |
| "检测红绿灯" | 红绿灯检测 |
| "帮我找一下矿泉水" | 物品搜索 |
| "帮我看看这是什么" | AI 拍照识别 |
| 任意问题 | AI 语音对话 |

### 6. 测试 AI 语音回复

1. 确认 `.env` 中已经配置 `DASHSCOPE_API_KEY`。
2. 启动后端：
   ```bash
   python app_main.py
   ```
3. 手机 App 连接到后端服务器。
4. 对手机说：“你好，你能听到我说话吗？”
5. 正常情况下：
   - 后端日志会出现 ASR final 文本。
   - Android 界面会显示 AI 回复。
   - 后端会调用阿里 DashScope/Qwen-Omni 生成语音，并通过 `/stream.wav` 推送到手机播放。
   - 如果阿里语音音频临时失败，手机会自动用 Android 系统 TextToSpeech 兜底播报文本。
6. 再说：“开始导航。”确认原有导航功能仍能启动。

### 7. AI 语音链路说明

当前语音链路：

1. Android 麦克风实时采集 16kHz PCM 音频。
2. App 通过 `/ws_audio` WebSocket 每 20ms 推送一段音频到后端。
3. 后端使用阿里 DashScope 实时 ASR 识别最终文本。
4. 后端把识别文本和最近摄像头画面交给阿里 Qwen-Omni 生成简短中文回复。
5. Qwen-Omni 返回的语音音频由后端转成 8kHz PCM，通过 `/stream.wav` 持续推给手机。
6. Android 端 `AudioPlaybackManager` 拉取 `/stream.wav` 并用 `AudioTrack` 播放。
7. 后端同时通过 `/ws_ui` 推送：
   ```json
   {"type":"ai_reply","text":"回复文本","tts_fallback":false}
   ```
   App 用它更新界面；当 `tts_fallback=true` 时才启用 Android 系统 TTS 兜底。

为降低识别延迟，Android 端会对很低音量的底噪做轻量静音门限处理，减少环境噪声导致 ASR 一直等待的情况。

---

## 项目结构

```
├── app_main.py              # 主入口，FastAPI 服务器
├── navigation_master.py     # 导航状态机
├── workflow_blindpath.py    # 盲道导航
├── workflow_crossstreet.py  # 过马路导航
├── yolomedia.py             # 物品搜索
├── asr_core.py              # 语音识别 (ASR)
├── omni_client.py           # AI 多模态对话
├── audio_player.py          # 音频播放
├── audio_stream.py          # 音频流推送
├── model/                   # AI 模型文件（必需）
├── music/                   # 导航提示音
├── voice/                   # 语音提示文件
├── android_app/             # Android 客户端源码
├── AIGlass-demo.apk         # Android 安装包
└── requirements.txt         # Python 依赖
```

## Android 客户端开发

用 Android Studio 打开 `android_app/` 目录即可编译修改。

技术栈：Kotlin + Jetpack Compose + CameraX + OkHttp WebSocket

## 模型文件说明

| 文件 | 用途 |
|---|---|
| yolo-seg.pt | 盲道分割 |
| yoloe-11l-seg.pt | 障碍物检测 |
| shoppingbest5.pt | 物品识别 |
| trafficlight.pt | 红绿灯检测 |
| hand_landmarker.task | 手部关键点 |

## 许可证

MIT License
